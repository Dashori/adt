version: "3.8"

services:
  redis:
    build:
      context: ./redis
      dockerfile: Dockerfile
    container_name: adt-redis
    ports:
      - 6379:6379
    networks:
      - mynetwork
  
  backend:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: adt-backend
    networks:
      - mynetwork
    depends_on:
      - redis

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: adt-nginx
    networks:
      - mynetwork
    ports:
      - 8089:80
    depends_on:
      - backend
      - redis


networks:
  mynetwork: